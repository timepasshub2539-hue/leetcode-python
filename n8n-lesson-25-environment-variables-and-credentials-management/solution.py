// Not secret, but shouldn't be hardcoded either:
{
  "API_BASE_URL": "https://staging.api.example.com",
  "FEATURE_FLAG_NEW_CHECKOUT": "true",
  "MAX_RETRIES": "3"
}
// These live as environment variables,
// separate from the credentials vault
